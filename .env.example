# =============================================================================
# ODONTOLINK - ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# INSTRUCCIONES:
# 1. Copia este archivo y renómbralo como .env
# 2. Completa TODOS los valores con tus credenciales reales
# 3. NUNCA subas el archivo .env al repositorio
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# Host de la base de datos MySQL (localhost para desarrollo, IP/dominio en producción)
DB_HOST=localhost

# Puerto de MySQL (por defecto 3306)
DB_PORT=3306

# Nombre de la base de datos
DB_NAME=odontolink

# Usuario de la base de datos
# RECOMENDACIÓN: NO usar 'root' en producción, crear un usuario específico
DB_USERNAME=root

# Contraseña de la base de datos
# RECOMENDACIÓN: Usar contraseñas fuertes (mín 16 caracteres, mayúsculas, minúsculas, números, símbolos)
DB_PASSWORD=your_secure_database_password_here

# -----------------------------------------------------------------------------
# JWT CONFIGURATION
# -----------------------------------------------------------------------------
# Secreto para firmar los tokens JWT
# CRÍTICO: DEBE ser único por entorno y MUY SEGURO
# RECOMENDACIÓN: Generar con: openssl rand -hex 64
# Mínimo 256 bits (64 caracteres hexadecimales)
JWT_SECRET=generate_a_secure_random_secret_key_here_minimum_64_chars

# Tiempo de expiración del token JWT en milisegundos
# 86400000 ms = 24 horas
# 3600000 ms = 1 hora
# 604800000 ms = 7 días
JWT_EXPIRATION=86400000

# -----------------------------------------------------------------------------
# SPRING PROFILES
# -----------------------------------------------------------------------------
# Perfil activo de Spring (dev, test, prod)
# dev: Desarrollo local con logs detallados
# test: Ambiente de pruebas
# prod: Producción con optimizaciones y seguridad máxima
SPRING_PROFILES_ACTIVE=dev

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# -----------------------------------------------------------------------------
# Puerto del servidor (por defecto 8080)
SERVER_PORT=8080

# -----------------------------------------------------------------------------
# SECURITY SETTINGS
# -----------------------------------------------------------------------------
# Allowed CORS origins (separados por coma)
# Ejemplo: http://localhost:3000,https://mi-app.com
# RECOMENDACIÓN: En producción, especificar SOLO los dominios permitidos
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200

# -----------------------------------------------------------------------------
# LOGGING (OPCIONAL)
# -----------------------------------------------------------------------------
# Nivel de logs (TRACE, DEBUG, INFO, WARN, ERROR)
# dev: DEBUG
# prod: WARN o ERROR
LOG_LEVEL=INFO
